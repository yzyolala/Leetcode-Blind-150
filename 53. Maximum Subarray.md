# 53. Maximum Subarray

贪心

```java
class Solution {
    public int maxSubArray(int[] nums) {
        int res=Integer.MIN_VALUE;
        int sum=0;
        for(int i:nums){
            sum+=i;
            if(sum>res) res=sum;
            if(sum<0) sum=0;//归零
        }
        return res;
    }
}
